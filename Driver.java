public class Driver {
  public static void main(STring[]args) {
    BankAccount sonic = new BankAccount (12340, 1230, "hedgehog");
    System.println ("Balance =" + sonic.getBalance());
    System.println ("AccountID =" + sonic.getAccountID());
    sonic.setPassword ("Lucario")
    if( sonic.withdraw(1000) ){
            println("Withdrawal success!");
       }else{
            println("Withdrawal failure");
       }
  }
  )
